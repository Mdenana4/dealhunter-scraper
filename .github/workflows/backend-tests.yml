name: Backend Tests & Lint

on:
  push:
    branches: [main, develop]
    paths:
      - 'server.py'
      - 'scraper.py'
      - 'requirements.txt'
      - '.github/workflows/backend-tests.yml'
  pull_request:
    branches: [main, develop]
    paths:
      - 'server.py'
      - 'scraper.py'
      - 'requirements.txt'

permissions:
  contents: read
  actions: read

jobs:
  lint:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0
          persist-credentials: false
        timeout-minutes: 5

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
        timeout-minutes: 10

      - name: Install linting tools
        run: |
          python -m pip install --upgrade pip
          pip install flake8 pylint

      - name: Lint with flake8
        run: |
          flake8 server.py scraper.py --count --select=E9,F63,F7,F82 --show-source --statistics || true
          flake8 server.py scraper.py --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics || true

  test-dependencies:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0
          persist-credentials: false
        timeout-minutes: 5

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
        timeout-minutes: 10

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Verify imports
        run: |
          python -c "import flask; import firebase_admin; import requests; print('✓ All dependencies installed successfully')"

  syntax-check:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0
          persist-credentials: false
        timeout-minutes: 5

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
        timeout-minutes: 10

      - name: Check Python syntax
        run: |
          python -m py_compile server.py
          python -m py_compile scraper.py
          echo "✓ Python syntax check passed"
